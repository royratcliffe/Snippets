# Ignore CocoaPods. See http://guides.cocoapods.org/using/using-cocoapods.html,
# section entitled "Should I check the Pods directory into source control?" for
# discussion. Benefits of ignoring: smaller source-control repo, no Pod merging
# conflicts.
Pods

# Swift's `build` command places products and caches in `.build` by default. You
# can change it using the `--build-path` option.
.build
Packages
